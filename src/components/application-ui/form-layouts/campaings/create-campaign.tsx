import { Card } from '@mui/material';
import CreateCampaingForm from './create-product-form';

const Component = () => {
  return (
    <Card>
      <CreateCampaingForm />
    </Card>
  );
};

export default Component;
